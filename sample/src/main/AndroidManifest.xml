<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cc.widget.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".NormalActivity" />
        <activity android:name=".PaddingActivity" />
        <activity android:name=".FormatActivity" />
        <activity android:name=".LeftLineActivity" />
        <activity android:name=".FontStyleActivity" />
        <activity android:name=".FamilyActivity" />
    </application>

</manifest>
